# @bilup/scratch-l10n 🍍
This repository contains translations for PineWarp projects.

## Scripts

This repository also contains scripts that we use to maintain PineWarp's translations. They assume you have a directory laid out with `scratch-l10n`, `scratch-gui`, `desktop`, and `packager` in the same parent folder.

build the translations:
```bash
npm run build
```